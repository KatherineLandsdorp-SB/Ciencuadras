#Driver
driverLocal=${DRIVER_LOCAL}
# Services
services.aut_base_url=${SERVICES_AUT_BASE_URL}
services.user_name=${SERVICES_USER_NAME}
services.password=${SERVICES_PASSWORD}
services.base_url=${SERVICES_BASE_URL}
services.base_path=${SERVICES_BASE_PATH}
services.scope.data_scope=${SERVICES_DATA_SCOPE}
services.scope.metrics_scope=${SERVICES_METRICS_SCOPE}

# Automation
automation.platformId=${AUTOMATION_PLATFORM_ID}
automation.platform_name = ${AUTOMATION_PLATFORM_NAME}
automation.environmentUrl=${AUTOMATION_ENVIRONMENT_URL}
automation.environmentId=${AUTOMATION_ENVIRONMENT_ID}
automation.execution.providerId=${AUTOMATION_PROVIDER_ID}
automation.execution.type_executionId=${AUTOMATION_TYPE_EXECUTION_ID}
automation.execution.type_automationId=${AUTOMATION_TYPE_AUTOMATION_ID}
automation.execution.type_automation_name = ${AUTOMATION_TYPE_AUTOMATION_NAME}
automation.execution.state_executionId=${AUTOMATION_STATE_EXECUTION_ID}
automation.execution.executor=${AUTOMATION_EXECUTOR_NAME}

# Capabilities WEB
web.capabilities.browserName =${WEB_CAPABILITIES_BROWSER_NAME}
web.capabilities.browserVersion =${WEB_CAPABILITIES_BROWSER_VERSION}
web.capabilities.platformOS =${WEB_CAPABILITIES_OS_PLATFORM}
web.capabilities.network =${WEB_CAPABILITIES_NETWORK}
web.capabilities.visual=${WEB_CAPABILITIES_VISUAL}
web.capabilities.video=${WEB_CAPABILITIES_VIDEO}
web.capabilities.console=${WEB_CAPABILITIES_CONSOLE}

# Configure authentication Lambda Test
lambda.userName=${LAMBDA_USER_NAME}
lambda.password=${LAMBDA_PASSWORD}
lambda.gridUrl=${LAMBDA_GRID_URL}

# Configure  authentication Jira
jira.trace_enable=${JIRA_TRACE_ENABLE}
jira.url=${JIRA_URL}
jira.host=${JIRA_HOST}
jira.port=${JIRA_PORT}
jira.user_name=${JIRA_USER}
jira.password=${JIRA_PASSWORD}
jira.name_project=${JIRA_NAME_PROJECT}
jira.id_epic_platform=${JIRA_ID_EPIC_PLATFORM}
jira.id_hu_project=${JIRA_ID_HU_PROJECT}
jira.user_name_responsible=${JIRA_USER_NAME_RESPONSIBLE}
